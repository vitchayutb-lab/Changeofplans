streamlit>=1.40,<2.0
pandas>=2.2
numpy>=1.26
plotly>=5.19
requests>=2.31

# สำหรับแชตบอทเวอร์ชัน Claude (ไม่บังคับ — ไม่ติดตั้งก็ใช้เส้นทางกฎได้)
anthropic>=0.40

# สำหรับรันชุดทดสอบ (ไม่จำเป็นต้องติดตั้งถ้าแค่เปิดเว็บ)
pytest>=8.0
